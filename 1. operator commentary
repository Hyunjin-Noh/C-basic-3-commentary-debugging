#include <stdio.h>

int main()
{
    int n, m, l; //정수형 변수 n, m, l 선언
    n = 5;
    m = 2; //n과 m을 각각 5와 2로 초기화
    l = n < m; // l에 n<m 관계연산결과값을 저장
    m = m * l; // m에 m*1 산술연산결과값을 저장
    l = 1; //l을 1로 초기화
    l = l < n; //l에 1<n 관계연산결과값을 저장
    printf("%d", l); //l 출력
}
